import { Outlet } from "react-router-dom";

const Inventory = () => {
    
    return (
        <div>
            <h2>Учет</h2>
            {/* <Outlet /> */}
        </div>
    );
};
export default Inventory;